---
author: Auto Fix Author
rating: 3
---

# Auto-Fix Demo

This note has no `status` field.
On validation, it should be **auto-fixed** to `new` (the default).
After auto-fix, the frontmatter will have `status: new` added automatically.
